(library
 (name web_test)
 (libraries
  jsip_web_components
  jsip_types
  jsip_parsing
  jsip_replay
  expect_test_helpers_core
  core)
 ; see lib/parsing/test/dune: work around this switch's ppx_expect
 ; mis-resolving dune's -source-tree-root
 (inline_tests
  (deps
   (glob_files_rec ../../../testing/*))
  (flags
   (:standard -source-tree-root .)))
 (preprocess
  (pps ppx_jane)))
