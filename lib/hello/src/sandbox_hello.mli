(** The sandbox library.

    A single example module to demonstrate the project's structure and
    conventions. Replace [Hello] with your own modules as the project grows,
    re-exporting each one here so the whole library is reachable through the
    top-level {!Sandbox_hello} module. *)

module Hello = Hello
